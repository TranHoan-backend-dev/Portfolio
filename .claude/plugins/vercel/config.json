{
    "plugin_name": "Vercel Deployer",
    "commands": {
        "deploy_preview": "vercel deploy",
        "deploy_prod": "vercel --prod",
        "check_logs": "vercel logs"
    },
    "rules": [
        "Always run 'pnpm build' before production deployment",
        "Verify environment variables in .env.production"
    ]
}